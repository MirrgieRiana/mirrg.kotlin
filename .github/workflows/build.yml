name: Build

on:
  push:
    branches:
      - main

jobs:
  main:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/setup-java@v2
      with:
        java-version: 11
        distribution: adopt

    - name: Build
      run: |
        bash gradlew build
